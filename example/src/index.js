import React from "react";
import ReactDOM from "react-dom";

import { Counter } from "konva-image-editor";

const App = (
  <div>
    <Counter />
  </div>
);

ReactDOM.render(App, document.getElementById("app"));
